export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">Agile & AI Knowledge Copilot</h1>
      <p className="text-neutral-500">
        Chat UI lands in build step 5, once the backend&apos;s RAG graph (step 3) exists.
      </p>
    </main>
  );
}
