# Copy this file to .env.local and fill in real values. Never commit .env.local.

# URL of the FastAPI backend (local dev default shown; production value is the Render URL)
NEXT_PUBLIC_API_URL=http://localhost:8000
